language: ruby
rvm:
  - jruby-9.1.6.0
  - 2.3.3
  - 2.2.6
  - 2.1.9
  - jruby-head
  - ruby-head
before_install: gem query -i -n ^bundler$ >/dev/null || gem install bundler

env:
  - JRUBY_OPTS="--dev --debug"

jdk:
  - oraclejdk8
